# Third-party notices

DIALibGen is BSD-3-Clause (see `LICENSE`). It builds against, and in
two places derives content from, the following third-party work.

## Derived content in this repository

### `include/odia/PeptDeepElements.h`

This header is a C++ transcription of AlphaPeptDeep's element mass and
composition constants. It was generated by `scripts/generate_peptdeep_elements.py`
from AlphaPeptDeep's `model_const.yaml`.

AlphaPeptDeep is licensed under the **Apache License, Version 2.0**. The table is
a derivative of that work; this notice is provided under Apache-2.0 §4(b) and
§4(d). The surrounding code is BSD-3-Clause.

- Upstream: https://github.com/MannLabs/alphapeptdeep
- Zeng *et al.*, "AlphaPeptDeep: a modular deep learning framework to predict
  peptide properties for proteomics", *Nat. Commun.* **13**, 7238 (2022).

### `data/peptdeep_meta_inputs.txt`, `data/peptdeep_mod_elements.txt`

Constants pinned from the same upstream, for the same reason and under the same
terms.

### `data/irt_standards.tsv`

The 11 Biognosys iRT standard peptides and their iRT values, as published in
Escher *et al.*, "Using iRT, a normalized retention time for more targeted
measurement of peptides", *Proteomics* **12**, 1111–1121 (2012). Sequence and
value data; cite the paper.

## Model weights — NOT distributed here

This repository contains **no** model weights. The three ONNX files the tool
predicts with (`peptdeep_{rt,ms2,ccs}_dynamic.onnx`) are AlphaPeptDeep models,
obtained separately — see README, "Models".

**The licence under which those published weights may be redistributed has not
been established by this project.** The copies available to us carried no
LICENSE, README or NOTICE, and the OpenMS install that downloads them records
only OpenMS's own licence. If you redistribute the weights — in a container
image, a conda package or otherwise — establish their terms first. Using them
locally is a different question from shipping them.

## Build dependencies

Linked against, not vendored:

| Component | Licence |
|---|---|
| OpenMS | BSD-3-Clause |
| Apache Arrow / Parquet | Apache-2.0 |
| ONNX Runtime | MIT |
| nlohmann/json | MIT |
| Boost (transitively, via OpenMS headers) | BSL-1.0 |

OpenMS itself pulls in further dependencies with their own terms, including
Coin-OR components under EPL-2.0. Consult your OpenMS distribution.

A **release bundle** carries the runtime libraries of that list alongside the
binary (that is what makes it runnable on a machine without a conda prefix), so
their terms travel with it. A source build links them and vendors nothing.

## Desktop app (`gui/`)

The desktop front-end is a separate dependency tree, none of it vendored here;
`gui/package-lock.json` and `gui/src-tauri/Cargo.lock` pin the exact versions.

| Component | Licence |
|---|---|
| Tauri 2 (and its plugins) | MIT or Apache-2.0 |
| React / React DOM | MIT |
| Vite, Vitest, TypeScript | MIT / Apache-2.0 |

A built app also embeds the OS's own webview (WebKit on macOS and Linux,
WebView2 on Windows), which is the platform's, not ours.
