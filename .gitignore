build/
build-*/
__pycache__/
*.pyc
*.o
*.a
*.so
*.parquet
*.tsv.gz
.cache/
compile_commands.json

# Release staging and the downloaded model weights, which are never committed
# (distributed in release bundles under the terms in THIRD-PARTY-NOTICES.md).
stage/
dist-bundles/
models/
